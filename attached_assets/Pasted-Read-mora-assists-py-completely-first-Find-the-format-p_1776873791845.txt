Read mora_assists.py completely first.
Find the format_picks_email() function.
Find the HTML email template inside it.
Report back the exact structure before
changing anything:

- Where does the picks section end
- What is the closing HTML after picks
- What is the footer section
- Does picks_data dict have a 
  bonus_plays key handled anywhere

DO NOT change anything yet.
Report only.

Then make ONE surgical addition:

Find where the HTML email template
ends its picks section and begins
the footer. It likely looks like
a closing div after the last pick card
followed by footer content.

Add this block between the last pick
and the footer — only if bonus_plays
exists in picks_data and has items:

{% if picks_data.get('bonus_plays') %}

<div style="max-width:600px;
            margin:32px auto 0;
            padding:0 20px;">

  <div style="background:#0F2406;
              border-radius:16px;
              padding:24px;
              border:1px solid
              rgba(76,187,23,0.3);">

    <p style="font-family:Arial,
              sans-serif;
              font-size:11px;
              font-weight:700;
              letter-spacing:2px;
              text-transform:uppercase;
              color:#4CBB17;
              margin:0 0 4px;">
      BONUS — NOT POWER PLAYS
    </p>

    <h3 style="font-family:Arial,
               sans-serif;
               font-size:22px;
               font-weight:900;
               color:#ffffff;
               text-transform:uppercase;
               margin:0 0 4px;">
      Total Bases 1.5+ Spots
    </h3>

    <p style="font-family:Arial,
              sans-serif;
              font-size:13px;
              color:#6B9E5A;
              margin:0 0 20px;
              line-height:1.5;">
      Contextual reads for deeper plays.
      Not part of the flat bet system —
      size these yourself.
    </p>

    {% for bonus in picks_data['bonus_plays'] %}
    <div style="border-top:1px solid
                rgba(107,158,90,0.2);
                padding:16px 0;">

      <div style="margin-bottom:6px;">
        <span style="font-family:Arial,
                     sans-serif;
                     font-size:18px;
                     font-weight:900;
                     color:#ffffff;">
          {{ bonus.player }}
        </span>
        <span style="font-family:Arial,
                     sans-serif;
                     font-size:12px;
                     color:#4CBB17;
                     font-weight:700;
                     margin-left:10px;">
          TB OVER {{ bonus.line }}
        </span>
      </div>

      <p style="font-family:Arial,
                sans-serif;
                font-size:12px;
                color:#6B9E5A;
                margin:0 0 4px;">
        {{ bonus.matchup }}
      </p>

      <p style="font-family:Arial,
                sans-serif;
                font-size:13px;
                color:#9CA3AF;
                margin:0;
                line-height:1.5;">
        {{ bonus.reason }}
      </p>

    </div>
    {% endfor %}

  </div>
</div>

{% endif %}

NOTE: If format_picks_email() builds
HTML as a Python string rather than
a Jinja2 template, use this Python
string version instead of Jinja2:

bonus_section = ''
if picks_data.get('bonus_plays'):
    bonus_section += '''
<div style="max-width:600px;
            margin:32px auto 0;
            padding:0 20px;">
  <div style="background:#0F2406;
              border-radius:16px;
              padding:24px;
              border:1px solid
              rgba(76,187,23,0.3);">
    <p style="font-family:Arial,sans-serif;
              font-size:11px;font-weight:700;
              letter-spacing:2px;
              text-transform:uppercase;
              color:#4CBB17;margin:0 0 4px;">
      BONUS — NOT POWER PLAYS
    </p>
    <h3 style="font-family:Arial,sans-serif;
               font-size:22px;font-weight:900;
               color:#ffffff;
               text-transform:uppercase;
               margin:0 0 4px;">
      Total Bases 1.5+ Spots
    </h3>
    <p style="font-family:Arial,sans-serif;
              font-size:13px;color:#6B9E5A;
              margin:0 0 20px;line-height:1.5;">
      Contextual reads for deeper plays.
      Not part of the flat bet system —
      size these yourself.
    </p>
'''
    for bonus in picks_data['bonus_plays']:
        bonus_section += f'''
    <div style="border-top:1px solid
                rgba(107,158,90,0.2);
                padding:16px 0;">
      <div style="margin-bottom:6px;">
        <span style="font-family:Arial,
                     sans-serif;font-size:18px;
                     font-weight:900;
                     color:#ffffff;">
          {bonus['player']}
        </span>
        <span style="font-family:Arial,
                     sans-serif;font-size:12px;
                     color:#4CBB17;
                     font-weight:700;
                     margin-left:10px;">
          TB OVER {bonus['line']}
        </span>
      </div>
      <p style="font-family:Arial,sans-serif;
                font-size:12px;color:#6B9E5A;
                margin:0 0 4px;">
        {bonus['matchup']}
      </p>
      <p style="font-family:Arial,sans-serif;
                font-size:13px;color:#9CA3AF;
                margin:0;line-height:1.5;">
        {bonus['reason']}
      </p>
    </div>
'''
    bonus_section += '''
  </div>
</div>
'''

Then insert bonus_section into the
HTML string just before the footer
closing content.

Find where html_body is assembled
and insert bonus_section at the
correct position — after picks,
before footer.

=== CONSTRAINTS ===
- Only touch mora_assists.py
- Only add the bonus section rendering
- Do not change any existing pick
  card HTML
- Do not change subject line logic
- Do not change SendGrid config
- Do not change subscriber loading
- Do not change any scheduler logic
- If bonus_plays is empty or missing
  from picks_data the section does
  not render at all — conditional only
- No duplicate sends
- No other files touched