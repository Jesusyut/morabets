Read dashboard.html completely first.
Four surgical changes only.
Do not touch anything else.

=== CHANGE 1: REMOVE THE INTRO OVERLAY ===

Find the element with id="mora-intro-overlay"
and all its contents.
Remove it entirely.

Also find and remove the JavaScript
that controls it:
- The localStorage check for mora_intro_seen
- The enterDashboard() function
- Any setInterval or setTimeout that
  references the overlay

Remove the overlay completely.
Dashboard loads clean for everyone
with no gate on first visit.


=== CHANGE 2: REMOVE THE EXPLAINER BAR ===

Find the sticky description bar added
at the top of the dashboard content.
It contains the text:
"Every prop below has the sportsbook's
margin removed."

Remove that entire div block.
Clean dashboard. No bar.


=== CHANGE 3: REMOVE MORA ASSISTS
SALES PAGE LINK ===

Find every link in dashboard.html
that points to /mora-assists.

There are likely two:
- One in the top navigation
- One in the Mora Assists popup

Remove the nav link to /mora-assists
from the navigation bar entirely.

The popup link gets replaced in
Change 4 below — do not remove
the popup itself.


=== CHANGE 4: REBUILD MORA ASSISTS
POPUP AS NEWSLETTER SIGNUP ===

Find the existing Mora Assists popup.
Keep the popup container and its
trigger timing logic exactly as is.
Only replace the inner HTML content.

The popup is now a newsletter signup.
Not a subscription sales pitch.
High converting. Soft ask. Free offer.

Replace the inner content with exactly:

<!-- MORA BETS NEWSLETTER POPUP -->

<!-- BADGE -->
<div style="text-align:center;
            margin-bottom:16px;">
  <span style="display:inline-block;
               background:rgba(76,187,23,0.15);
               border:1px solid #4CBB17;
               border-radius:100px;
               padding:4px 16px;
               font-family:'Inter',sans-serif;
               font-size:11px;
               font-weight:700;
               letter-spacing:1.5px;
               text-transform:uppercase;
               color:#4CBB17;">
    FREE UNTIL SEPTEMBER 1ST
  </span>
</div>

<!-- HEADLINE -->
<h2 style="font-family:'Barlow Condensed',
           sans-serif;
           font-size:34px;
           font-weight:900;
           text-transform:uppercase;
           color:#ffffff;
           text-align:center;
           line-height:1.05;
           margin:0 0 10px;">
  The Math Is Expanding.
  <span style="color:#4CBB17;">
    Stay Ahead.
  </span>
</h2>

<!-- SUBTEXT -->
<p style="font-family:'Inter',sans-serif;
          font-size:14px;
          color:#6B9E5A;
          text-align:center;
          line-height:1.6;
          margin:0 0 20px;">
  UFC, Soccer, and NBA no-vig
  analysis is coming to Mora Bets.
  Drop your email and be the first
  to know when it goes live.
  <strong style="color:#ffffff;">
    Free until September 1st.
  </strong>
</p>

<!-- COMING SOON ROW -->
<div style="display:flex;
            justify-content:center;
            gap:8px;
            margin-bottom:20px;
            flex-wrap:wrap;">

  <span style="font-family:'Inter',
               sans-serif;
               font-size:11px;
               font-weight:600;
               color:#6B9E5A;
               background:rgba(255,255,255,0.05);
               border:1px solid rgba(107,158,90,0.3);
               border-radius:100px;
               padding:4px 12px;">
    ⚡ MLB — Live Now
  </span>

  <span style="font-family:'Inter',
               sans-serif;
               font-size:11px;
               font-weight:600;
               color:#6B9E5A;
               background:rgba(255,255,255,0.05);
               border:1px solid rgba(107,158,90,0.3);
               border-radius:100px;
               padding:4px 12px;">
    🥊 UFC — Coming Soon
  </span>

  <span style="font-family:'Inter',
               sans-serif;
               font-size:11px;
               font-weight:600;
               color:#6B9E5A;
               background:rgba(255,255,255,0.05);
               border:1px solid rgba(107,158,90,0.3);
               border-radius:100px;
               padding:4px 12px;">
    ⚽ Soccer — Coming Soon
  </span>

  <span style="font-family:'Inter',
               sans-serif;
               font-size:11px;
               font-weight:600;
               color:#6B9E5A;
               background:rgba(255,255,255,0.05);
               border:1px solid rgba(107,158,90,0.3);
               border-radius:100px;
               padding:4px 12px;">
    🏀 NBA — Coming Soon
  </span>

</div>

<!-- EMAIL INPUT -->
<input id="newsletter-email-input"
       type="email"
       placeholder="your@email.com"
       style="width:100%;
              border:1.5px solid
              rgba(107,158,90,0.4);
              border-radius:12px;
              padding:14px 16px;
              font-family:'Inter',sans-serif;
              font-size:15px;
              background:rgba(255,255,255,0.05);
              color:#ffffff;
              box-sizing:border-box;
              outline:none;
              margin-bottom:10px;"
       onfocus="this.style.borderColor='#4CBB17'"
       onblur="this.style.borderColor=
               'rgba(107,158,90,0.4)'">

<!-- CTA BUTTON -->
<button onclick="submitNewsletter()"
        style="width:100%;
               background:#4CBB17;
               color:#0F2406;
               font-family:'Barlow Condensed',
               sans-serif;
               font-size:19px;
               font-weight:900;
               text-transform:uppercase;
               letter-spacing:1px;
               padding:16px;
               border:none;
               border-radius:12px;
               cursor:pointer;
               box-shadow:0 4px 20px
               rgba(76,187,23,0.3);
               margin-bottom:10px;">
  Notify Me When It Drops →
</button>

<!-- SUCCESS STATE (hidden) -->
<div id="newsletter-success"
     style="display:none;
            text-align:center;
            padding:16px;
            background:rgba(76,187,23,0.1);
            border-radius:12px;
            margin-bottom:10px;">
  <p style="font-family:'Inter',sans-serif;
            font-size:14px;
            color:#4CBB17;
            font-weight:600;
            margin:0;">
    ✅ You are in.
    We will hit your inbox first.
  </p>
</div>

<!-- DISMISS -->
<div style="text-align:center;">
  <button onclick="closeNewsletterPopup()"
          style="background:none;
                 border:none;
                 color:#6B9E5A;
                 font-family:'Inter',
                 sans-serif;
                 font-size:12px;
                 cursor:pointer;
                 padding:4px;">
    No thanks
  </button>
</div>


=== ADD THIS JAVASCRIPT ===

Add these functions in a <script> block
in dashboard.html:

async function submitNewsletter() {
  const email = document.getElementById(
    'newsletter-email-input'
  ).value.trim();

  if (!email || !email.includes('@')) {
    document.getElementById(
      'newsletter-email-input'
    ).style.borderColor = '#EF4444';
    return;
  }

  try {
    await fetch(
      'https://hooks.zapier.com/hooks/catch/22797974/uj2zhly/',
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          email: email,
          source: 'newsletter_popup',
          timestamp: new Date().toISOString()
        })
      }
    );
  } catch(e) {
    console.log('Submit error:', e);
  }

  document.getElementById(
    'newsletter-email-input'
  ).style.display = 'none';

  document.querySelector(
    '[onclick="submitNewsletter()"]'
  ).style.display = 'none';

  document.getElementById(
    'newsletter-success'
  ).style.display = 'block';

  localStorage.setItem(
    'mora_newsletter_signed_up', 'true'
  );

  setTimeout(function() {
    closeNewsletterPopup();
  }, 3000);
}

function closeNewsletterPopup() {
  const popup = document.getElementById(
    'mora-assists-modal'
  );
  if (popup) {
    popup.style.opacity = '0';
    popup.style.transition = 'opacity 0.3s';
    setTimeout(function() {
      popup.style.display = 'none';
    }, 300);
  }
  localStorage.setItem(
    'mora_assists_last_shown',
    Date.now().toString()
  );
}

Update the existing popup show logic
so it also checks:
If mora_newsletter_signed_up is true
in localStorage — never show the popup
again for that browser.

Add this check alongside the existing
mora_assists_last_shown check:

const signedUp = localStorage.getItem(
  'mora_newsletter_signed_up'
);
if (signedUp) return;


=== CONSTRAINTS ===
- Only touch dashboard.html
- Do not touch app.py
- Do not touch mora_assists_sales.html
  — that page stays deployed but is
  simply unlinked from the dashboard
- Do not touch mora_assists.py
- Do not touch email gate
- Do not touch Meta pixel
- Do not touch odds fetching
- Do not touch color scheme
- Do not touch Bovada footer
- Popup timing logic stays exactly
  the same — only inner content changes
- Zapier webhook is the same one
  used for Friday email capture
- source field is 'newsletter_popup'
  so you can track in Google Sheets
- Once someone submits their email
  the popup never shows again
  for that browser
- No changes to any other modals
  or popups on the page