# REPLIT AGENT PROMPT — FIX MATCHUP FILTER + PUSH TO GITHUB
# Paste this entire prompt into the Replit Agent chat

---

## PROMPT START

I have a Python sports betting app called Mora Bets. Props are loading from
mlb_props_cache.json but not displaying because a date filter is blocking them.
The props have no date field so the filter returns zero matches every time.

Your job is to:
1. Find every place in the codebase that filters props by date before or during matchup filtering
2. Remove ONLY the date condition from those filters — keep all other filtering logic intact
3. Keep the team name matching logic exactly as-is
4. Commit the fix and push to the connected GitHub repository

---

## WHAT TO FIND

Search the entire codebase for patterns like these and remove the date condition:

PATTERN 1 — date field check in a list comprehension:
  BEFORE: [p for p in props if p['date'] == today and p['team'] in live_teams]
  AFTER:  [p for p in props if p['team'] in live_teams]

PATTERN 2 — date field with .get():
  BEFORE: [p for p in props if p.get('date') == today and ...]
  AFTER:  [p for p in props if ...]  (remove only the date condition)

PATTERN 3 — date filter as a separate step before matchup filter:
  BEFORE:
    todays_props = [p for p in props if p['date'] == today]
    matchups = [p for p in todays_props if p['team'] in live_teams]
  AFTER:
    matchups = [p for p in props if p['team'] in live_teams]

PATTERN 4 — date comparison using datetime:
  BEFORE: if prop['game_date'] == datetime.date.today().isoformat():
  AFTER:  remove this entire condition, keep the team name check

PATTERN 5 — strict matchup filter function that checks date:
  Find any function named like: strict_match, filter_matchups, get_matchups,
  filter_props, get_todays_props, or similar
  Inside that function, remove any line that compares a date field to today's date
  Leave all other logic in that function completely untouched

---

## SEARCH COMMANDS TO RUN FIRST

Run these in the Shell to locate the exact files before editing:

  grep -rn "game_date\|prop.*date\|date.*prop\|today.*match\|matchup" . --include="*.py" | grep -v "__pycache__"
  grep -rn "Available matchups\|strict.*match\|filter.*prop\|todays_prop" . --include="*.py"

Read the output carefully. Open every file that appears in the results.
Read the full function context around each match before making any changes.

---

## RULES FOR EDITING

- Remove ONLY date-related conditions from filters
- Do NOT remove or change any team name matching logic
- Do NOT remove or change any odds, confidence, or stat filtering
- Do NOT change any function signatures or return types
- Do NOT touch any unrelated code
- If a variable like `today` or `current_date` is only used for the date filter
  you are removing, also remove that variable declaration to keep the code clean
- If removing the date condition makes an `and` into nothing, clean up the syntax:
    BEFORE: if date_ok and team in live_teams:
    AFTER:  if team in live_teams:

---

## AFTER MAKING THE CHANGES

1. Run this to verify syntax is clean:
   python3 -m py_compile <each file you changed>
   If any file fails, fix the syntax error before continuing.

2. Do a quick sanity check — run:
   grep -n "game_date\|prop\['date'\]\|prop\.get('date')" <changed files>
   Confirm the date conditions are gone.

3. Commit and push to GitHub:
   git add -A
   git commit -m "fix: remove date filter from matchup logic — match on team names only"
   git push origin main

4. Confirm the push succeeded by checking git status:
   git status
   git log --oneline -3

Report back:
- Which files were changed
- Exactly which lines were modified (show before and after for each)
- The git commit hash after pushing
- Any errors encountered

## PROMPT END