This is a LIVE production app with active
users. Zero tolerance for breaking anything
currently working. Read every file completely
before touching anything. Report findings first.

Three issues to fix. Do them in this order,
verifying after each one before moving to
the next. Do not batch all three together.

═══════════════════════════════════════
ISSUE 1 — LINE SHOPPING BROKEN AGAIN
═══════════════════════════════════════

The "Line Shop (X books)" expand button
on prop cards is not functioning. This was
reportedly fixed previously but regressed.

STEP 1: Read the exact current code for
the line shop button and its click handler
in dashboard.html. Report the current state
before changing anything.

STEP 2: Identify what broke it. Common
causes to check specifically:
- Does the button's onclick reference a
  dynamic id that includes the line value
  (e.g. id="lineshop-${player}-${line}")
  where a decimal like 1.5 or special
  characters in player names break the
  selector or id lookup
- Is there a duplicate id on the page
  causing getElementById to grab the
  wrong element
- Did the soccer tab addition introduce
  a naming collision with MLB/NHL
  line shop elements using the same ids
- Is the click handler still attached
  after any dynamic re-render of the
  props grid (event listener lost on
  innerHTML replacement)

STEP 3: Fix using event delegation
instead of inline onclick where possible.
Attach ONE click listener to the parent
props container that checks if the
clicked element or its parent has the
line-shop-toggle class, rather than
attaching individual onclick handlers
to every card. This prevents the bug
from recurring when cards re-render.

STEP 4: Test on MLB tab AND Soccer tab
specifically — confirm both work since
the regression may be sport-specific
given Soccer was added most recently.

Report exact root cause found and
exact fix applied before proceeding
to Issue 2.

═══════════════════════════════════════
ISSUE 2 — SOCCER CARDS MISSING TAGS
THAT MLB HAS
═══════════════════════════════════════

MLB cards display three things Soccer
cards currently lack:

1. HIGH SCORING / LOW SCORING environment tag
2. Favored team win probability badge
   (glowing green percentage block)

STEP 1: Read how MLB calculates and
displays the environment tag (HIGH
SCORING / LOW SCORING). Find the exact
function and the data field it reads
(likely something like environment_label
or similar on the game/matchup object).

STEP 2: Read how MLB calculates the
favored-team moneyline badge (the
glowing green 70%+ style badge shown
on team matchup cards, not player prop
cards — confirm this is the GAME card
not the PLAYER PROP card before building).

STEP 3: For Soccer, the equivalent
environment signal should be based on
combined team goal totals/over-under
context if available from the same
markets already being fetched. If game-level
totals are not currently fetched for
soccer, do NOT fabricate this — report
back what data is actually available
before adding fake tags.

STEP 4: For the favored team badge,
this requires the soccer game-level
moneyline/h2h market — which leads
directly to Issue 3 below. This badge
cannot be added correctly until Issue 3
is resolved, because there is currently
no win probability data being fetched
for soccer games at all (only player
props). Confirm this dependency in your
report before attempting Issue 2 fully.

Do not skip ahead — Issue 3 must be
fixed first for Issue 2 to be completed
correctly with real data.

═══════════════════════════════════════
ISSUE 3 — SOCCER GAME LINES NOT
CALLING THE ODDS API AT ALL
═══════════════════════════════════════

This is the most important fix. Soccer
currently only fetches PLAYER PROPS.
Game-level lines (moneyline/h2h, totals,
spreads) are not being fetched at all
for soccer — unlike MLB and NHL which
have both player props AND game lines.

STEP 1: Read odds_api.py and find the
exact function that fetches MLB game
lines (moneyline, run line, totals) —
this is separate from the player props
function. Report its name and structure.

STEP 2: Read nhl_odds_api.py and confirm
it mirrors the same pattern for NHL
game lines.

STEP 3: Create the equivalent function
in soccer_odds_api.py:

def fetch_soccer_game_lines():
    """
    Fetches game-level lines for MLS and
    FIFA World Cup: h2h (moneyline/3-way),
    totals (over/under goals), and spreads
    if available. Separate from player props.
    """

Use the SAME sport keys already working
for player props:
    soccer_usa_mls
    soccer_fifa_world_cup

Market keys for soccer game lines:
    h2h (3-way moneyline: home/draw/away)
    totals (over/under total goals)
    spreads (if offered — soccer spreads
    are less common, fetch if available,
    skip gracefully if not)

This is a SEPARATE, SIMPLER API call
than player props — it does NOT require
the event-by-event loop. Mirror the MLB
game lines fetch pattern exactly:

GET https://api.the-odds-api.com/v4/sports/{sport_key}/odds
params: apiKey={ODDS_API_KEY}
        regions=us
        markets=h2h,totals
        oddsFormat=american
        bookmakers=draftkings,fanduel,betmgm,betrivers

This is ONE call per sport_key, not
per-event, since the /odds endpoint
returns all games with their lines
in a single response. Confirm this
matches how MLB game lines are fetched
— report if MLB does it differently.

STEP 4: Calculate no_vig_probability
for h2h three-way markets. Soccer h2h
has THREE outcomes (home win, draw,
away win) not two like MLB moneylines.
Check if probability.py's no_vig_probability()
function supports 3-way markets. If it
only supports 2-way (over/under or
team A/team B), report this back —
a 3-way no-vig calculation needs:

implied_home = price_to_prob(home_price)
implied_draw = price_to_prob(draw_price)
implied_away = price_to_prob(away_price)
total = implied_home + implied_draw + implied_away
no_vig_home = implied_home / total
no_vig_draw = implied_draw / total
no_vig_away = implied_away / total

Do not write this calculation without
confirming probability.py's current
function signature first — report back
whether it needs a new 3-way variant
or can be reused.

STEP 5: Cache to a separate file:
/var/data/soccer_gamelines_cache.json
Keep this separate from
soccer_props_cache.json (player props)
so neither overwrites the other.

STEP 6: Add this fetch to the SAME
scheduler slot as the player props
fetch (7:45 AM PHX) — call both
functions in sequence within the
same safe wrapper:

def fetch_soccer_props_safe():
    try:
        from soccer_odds_api import (
            fetch_soccer_props,
            fetch_soccer_game_lines
        )
        props = fetch_soccer_props()
        gamelines = fetch_soccer_game_lines()
        logger.info(
            f'Soccer: {len(props)} props, '
            f'{len(gamelines)} game lines cached'
        )
    except Exception as e:
        logger.error(
            f'Soccer fetch failed: {e}'
        )

STEP 7: Add a new dashboard endpoint
/api/soccer/gamelines mirroring whatever
endpoint MLB uses for its game lines
("Best Lines" tab data) — report the
exact MLB pattern before building this.

═══════════════════════════════════════
VERIFICATION BEFORE TOUCHING DASHBOARD UI
═══════════════════════════════════════

After Issue 3 backend is built run this
verification BEFORE wiring any frontend:

python3 -c "
import json, sys
sys.path.insert(0, '.')
from soccer_odds_api import fetch_soccer_game_lines

print('Fetching soccer game lines...')
lines = fetch_soccer_game_lines()
print(f'Total game lines: {len(lines)}')

if lines:
    for l in lines[:5]:
        print(f'  {l.get(\"home_team\")} vs {l.get(\"away_team\")} | no_vig_home: {l.get(\"no_vig_home\")}% | environment: {l.get(\"environment\", \"NOT SET\")}')
else:
    print('No game lines returned — check sport_key activity')
"

Report this output before proceeding
to add the HIGH SCORING / LOW SCORING
tag and favored-team badge to the
Soccer UI from Issue 2.

═══════════════════════════════════════
SAFETY CONSTRAINTS — LIVE APP
═══════════════════════════════════════

- This app has active paying subscribers
  and live traffic. Every change must be
  additive or surgical — never delete
  working MLB/NHL code paths
- Do not touch the MLB or NHL fetch
  functions, scheduler jobs, or cache
  files in any way
- Do not touch mora_assists.py
- Do not touch email gate
- Do not touch Meta pixel or GTM
- Do not touch the Bovada footer
- Do not touch the newsletter popup
  built in the previous session
- Test each issue fix independently
  before moving to the next
- If Issue 1's root cause turns out to
  affect MLB/NHL line shop too, fix it
  globally using event delegation — but
  confirm this doesn't change any other
  card behavior first
- Report root cause and fix for each
  issue separately — do not bundle
  reports together
- If any step requires removing or
  significantly restructuring existing
  working code, stop and report back
  before proceeding rather than risk
  breaking live functionality