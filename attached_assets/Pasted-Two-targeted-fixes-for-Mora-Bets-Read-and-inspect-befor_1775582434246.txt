Two targeted fixes for Mora Bets.
Read and inspect before changing anything.

DO NOT touch MLB pipeline, EV engine,
odds fetching, affiliate links, email gate,
Meta pixel, TikTok pixel, or color scheme.

=== STEP 1: INSPECT MLB GAME CONTEXT 
PIPELINE COMPLETELY ===

Before writing a single line, read and 
understand exactly how MLB produces the
"LOW SCORING", "HIGH SCORING" labels and
the green favored team highlight.

INSPECT 1 — Find where environment labels 
come from in the MLB props display:

Search all template files for:
- "LOW SCORING"
- "HIGH SCORING" 
- "LOW_SCORING"
- "HIGH_SCORING"
- "environment"
- "favored"
- "favored_team"

Report which template file contains these
and how they are rendered in the HTML.

INSPECT 2 — Find the backend that produces
environment data for MLB:

Search app.py and all Python files for:
- get_mlb_game_environment_map
- get_mlb_stats_environment_map
- classify_game_environment
- "environment"
- "favored_team"
- "low scoring"
- "high scoring"

Report which function produces this data
and what it returns exactly.

INSPECT 3 — Read the full environment 
function found in INSPECT 2:

Read every line of whatever function 
generates the environment label.
Report:
- What inputs does it take?
- What does it return? Show example output.
- Where does it get the data from?
- How does it decide Low/High scoring?
- How does it determine favored team?

INSPECT 4 — Find where MLB props route
attaches environment data to each game:

In app.py find /api/mlb/props or 
_fetch_and_process_mlb_props or wherever
environment data gets attached to props.
Report exactly how environment label and
favored_team get added to each prop dict.

INSPECT 5 — Read the MLB frontend rendering:

In the dashboard template find the code
that renders the MLB matchup list with
the game cards showing team names,
prop counts, environment badges, and
favored team highlighting.

Report:
- What field names does the JS read?
  (e.g. prop.environment, prop.favored_team)
- How is the green highlight applied 
  to the favored team abbreviation?
- How is the LOW SCORING badge rendered?
- What is the exact HTML/CSS structure?

INSPECT 6 — Read NHL frontend rendering:

Find where NHL props are rendered in
the dashboard. Compare to MLB rendering.
Report what fields the NHL display 
currently reads and what is missing
compared to MLB.

INSPECT 7 — Verify NHL game odds function:

In nhl_odds_api.py find fetch_nhl_game_odds()
Report what it returns — does it include
enough data to calculate scoring environment
and favored team the same way MLB does?

=== STEP 2: REPORT FINDINGS ===

After all inspections report:

1. Exact function that produces MLB 
   environment labels — name and file
2. Exact fields added to each MLB prop:
   environment, favored_team, etc
3. Exact HTML structure of the MLB
   game card with green highlight
4. What NHL is missing vs MLB
5. Whether fetch_nhl_game_odds() returns
   enough data to replicate the feature

=== STEP 3: ADD ENVIRONMENT LABELS 
TO NHL — MIRROR MLB EXACTLY ===

Only after Steps 1 and 2 are complete.

Using the exact same pipeline discovered
for MLB, apply it to NHL.

The NHL equivalent logic:

SCORING ENVIRONMENT for NHL:
NHL totals typically range 5.0 to 7.5 goals
Use the same classify function MLB uses
but with NHL-appropriate thresholds:

If the total line exists from NHL game odds:
- Under 5.5 goals = "LOW SCORING"
- 5.5 to 6.5 goals = neutral (no badge)  
- Over 6.5 goals = "HIGH SCORING"

If the classify_game_environment function
already handles this correctly based on
the total line value, use it as-is.
Do not rewrite it — call it the same way
MLB calls it.

FAVORED TEAM for NHL:
Same as MLB — whichever team has the
lower (more negative) moneyline odds
is the favorite. Highlight their 
abbreviation in kelly green.

ATTACHING TO NHL PROPS:
Wherever MLB attaches environment and
favored_team to each prop dict,
do the exact same thing for NHL props
using fetch_nhl_game_odds() as the
data source.

FRONTEND RENDERING:
Apply the exact same HTML/CSS structure
found in INSPECT 5 to the NHL game cards.
Same badge. Same green highlight.
Same field names.
Zero visual difference between MLB and NHL
environment display.

=== FIX 2: DEFAULT FILTER TO ALL TIERS ===

This is a one-line JavaScript change.

Find where the tier filter dropdown or
button group initializes its default
selected value in dashboard.html.

It currently defaults to "LOCK" or a
specific tier on page load.

Find the initialization code — it will
look something like one of these:

  let activeTier = 'LOCK';
  OR
  selectedTier = 'LOCK';
  OR
  filterByTier('LOCK');
  OR
  document.querySelector('[data-tier="LOCK"]')
    .classList.add('active');

Change EVERY instance of the default
tier value from 'LOCK' to 'ALL' or 
whatever value the "All Tiers" option uses.

Also find where the filter is applied
on tab switch — when a user clicks MLB,
NHL, or NFL tab the filter should reset
to All Tiers not carry over a previous
tier selection.

Add this reset wherever tab switching occurs:

// Reset to All Tiers on tab switch
activeTier = 'ALL'; // or whatever all value is
// Update the UI to show All Tiers selected
updateTierFilterUI('ALL');

Apply this change for ALL sport tabs.
MLB, NHL, and NFL all default to All Tiers.

=== STEP 4: VERIFY ===

After changes, run in shell:

python3 -c "
import json

# Check NHL props have environment fields
with open('nhl_props_cache.json') as f:
    nhl = json.load(f)

with open('mlb_props_cache.json') as f:
    mlb = json.load(f)

# Find environment field name from MLB
mlb_sample = mlb[0] if mlb else {}
nhl_sample = nhl[0] if nhl else {}

print('MLB environment fields:')
for k, v in mlb_sample.items():
    if any(x in k.lower() for x in 
           ['env', 'favor', 'scor']):
        print(f'  {k}: {v}')

print('NHL environment fields:')
for k, v in nhl_sample.items():
    if any(x in k.lower() for x in 
           ['env', 'favor', 'scor']):
        print(f'  {k}: {v}')
"

=== CONSTRAINTS ===
- Read MLB pipeline completely before 
  touching NHL — no exceptions
- Do NOT rewrite classify_game_environment
  if it already exists — call it as-is
- Do NOT change fetch_player_props() 
  in either odds_api.py or nhl_odds_api.py
- Do NOT change group_props_by_player()
- Do NOT change probability.py
- Do NOT change any existing MLB routes
  or MLB rendering logic
- The All Tiers default is ONE change —
  find the exact variable/value and 
  change only that, nothing else
- Do NOT redesign the NHL card layout —
  add the environment badge and favored
  highlight to the existing card structure
- Zero unnecessary line changes anywhere