Read app.py completely.
Find the /api/context-edge route.
Find where SYSTEM_PROMPT is built
as an f-string inside the route.

Report the exact line count of the
SYSTEM_PROMPT string and whether
format_board_for_claude(board) is
called directly inside the f-string
or assigned to a variable first.

Then make this one surgical fix:

=== FIX: PRE-BUILD BOARD STRING
BEFORE F-STRING ===

Find this pattern inside the route
where the system prompt is built:

SYSTEM_PROMPT = f"""...
{format_board_for_claude(board)}
..."""

The function call inside the f-string
may be failing silently when the board
is large (11,455 chars) inside a
Flask request context.

Replace with a two-step approach:

Step 1 — call the function first
and assign to a variable:

board_text = format_board_for_claude(board)

Step 2 — use the variable in the
f-string not the function call:

SYSTEM_PROMPT = f"""...
{board_text}
..."""

Also add today_str pre-assignment
the same way — confirm it is assigned
as a variable BEFORE the f-string,
not called inline inside it.

The f-string should contain ONLY
simple variable references like
{board_text} and {today_str}.
No function calls inside the f-string.
No __import__() calls.
No method calls.
Only pre-assigned variables.

=== ALSO ADD ERROR VISIBILITY ===

Find the except block at the bottom
of the route that catches all errors.

Change it to log the full traceback:

except Exception as e:
    import traceback
    tb = traceback.format_exc()
    logger.error(
        f'Context edge error: {tb}'
    )
    return jsonify(
        {'error': 'Analysis failed. Try again.'}
    ), 500

This will show the exact crash line
in Render logs the next time it fails.

=== CONSTRAINTS ===
- Only touch the /api/context-edge route
- Only pre-assign board_text variable
- Only confirm today_str is pre-assigned
- Only update the except block logging
- Do not touch any other part of app.py
- Do not touch dashboard.html
- Do not touch mora_assists.py
- Do not touch scheduler jobs
- Do not touch Bovada footer
- Do not touch newsletter popup