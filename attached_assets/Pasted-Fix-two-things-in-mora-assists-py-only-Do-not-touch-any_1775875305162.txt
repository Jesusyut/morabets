Fix two things in mora_assists.py only.
Do not touch any other file.

=== FIX 1: UPDATE LLM PERSONA IN 
SELECTION_PROMPT ===

Find the very beginning of SELECTION_PROMPT.
It currently starts with something like:

"You are the Mora Assists pick selector."

Replace that opening line with:

"You are a professional data analyst,
the best in the market for oddsmakers.
You know the right proportions.
You are the Mora Assists pick selector."

Keep everything else in the prompt
exactly as it is.

=== FIX 2: CLARIFY ANCHOR DEFINITION 
IN SELECTION_PROMPT ===

Find the section in SELECTION_PROMPT
that defines anchor picks. It currently
says something about "straight plays"
or "game lines."

Replace the anchor section description
with this clarified version:

═══════════════════════════════════════
ANCHOR RULES (Picks 3, 4, and 5)
═══════════════════════════════════════

Anchors are ANY bet that is NOT a 
player prop. This includes:

- Moneyline (team to win outright)
- Run line / Puck line / Spread
  (team to cover the spread)
- Game totals (over/under total
  runs, goals, or points scored)
- Any other game-level market
  available on the board

DO NOT limit anchors to one market type.
If the board has moneylines use them.
If it has totals use them.
If it has spreads use them.
Mix across market types if that gives
the best probability picks.

The board data you receive includes
BOTH props and lines. Anchors come
from the lines section of the board.

If the lines section appears empty,
look inside the props data for any
game-level entries that are not
player-specific — some game lines
may be mixed into the props feed.

Minimum no_vig_prob: 60%
Hard ceiling: -220 juice maximum

JUICE EFFICIENCY — same rules as before:
Tier 1: Positive odds above 55% — take immediately
Tier 2: -110 to -160 at 60-64% — strong yes
Tier 3: -160 to -200 at 63-67% — yes
Tier 4: -200 at 60% — last resort only
Tier 5: Above -220 — never

3 anchors from 3 different games.
No game already used in prop picks 1 or 2.
Spread across sports when possible.

If fewer than 3 qualifying anchors exist
today, fill remaining slots with the
best available props above 60% probability
rather than sending empty anchor slots
with odds: 0 and prob: 0.0.

NEVER return an anchor pick with
odds: 0 or no_vig_prob: 0.0 —
that means no data was found.
Replace empty anchors with bonus
prop picks above 60% instead.

=== FIX 3: UPDATE load_full_board()
TO CONFIRM LINES ARE LOADING ===

In mora_assists.py find load_full_board().

After loading props, add explicit logging
to confirm lines cache files are being read:

# Check all possible lines cache files
lines_files = [
    '/var/data/mlb_lines_cache.json',
    '/var/data/nhl_lines_cache.json',
    '/var/data/nfl_lines_cache.json',
    'mlb_lines_cache.json',
    'nhl_lines_cache.json',
]

for cache_file in lines_files:
    try:
        lines = load_props_from_file(cache_file)
        if lines:
            board["lines"].extend(lines)
            logger.info(
                f"[BOARD] Loaded {len(lines)} "
                f"lines from {cache_file}"
            )
    except Exception:
        pass

# If lines still empty after checking files
# pull directly from the API cache in memory
if not board["lines"]:
    logger.warning(
        "[BOARD] No lines cache found — "
        "LLM will use props only for anchors"
    )

=== CONSTRAINTS ===
- Only change mora_assists.py
- Do not touch dashboard.html
- Do not touch app.py
- Do not touch any odds fetching
- Do not touch email formatting
- Do not touch the casual picks section
- The persona change is the first two
  lines of SELECTION_PROMPT only
- The anchor fix clarifies definition
  and adds the fallback rule so
  picks 3-5 never return empty zeros