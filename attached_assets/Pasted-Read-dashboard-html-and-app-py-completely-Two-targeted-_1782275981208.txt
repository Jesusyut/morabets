Read dashboard.html and app.py completely.
Two targeted changes only.
Do not touch anything else.

═══════════════════════════════════════
CHANGE 1 — dashboard.html
REPLACE QUICK PROMPT BUTTONS
═══════════════════════════════════════

Find the welcome message div inside
id="ce-messages" that contains the
four quick prompt buttons:
- best bets today
- top 5 plays
- best value odds
- World Cup edges

Replace those four buttons with
these seven sport-specific buttons:

<div style="
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 8px;
">

  <button onclick="quickPrompt('best value bets today MLB — include moneylines, spreads, and player props')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">⚾ MLB value</button>

  <button onclick="quickPrompt('best value bets today Soccer World Cup — include moneylines and totals')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">⚽ Soccer value</button>

  <button onclick="quickPrompt('top 5 plays today across all sports — mix of player props, moneylines, and spreads')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">⚡ Top 5 plays</button>

  <button onclick="quickPrompt('best plus money plays today — any sport, odds better than +100')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">💰 Plus money</button>

  <button onclick="quickPrompt('best MLB moneyline and run line bets today based on pitching matchups and park factors')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">📊 MLB lines</button>

  <button onclick="quickPrompt('World Cup match result and total goals bets today — best context edges')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">🏆 World Cup</button>

  <button onclick="quickPrompt('best high scoring game total plays today — overs with park factor and pitching context')"
    style="
      background: rgba(76,187,23,0.1);
      border: 1px solid rgba(76,187,23,0.3);
      border-radius: 100px;
      padding: 4px 10px;
      font-family: 'Inter', sans-serif;
      font-size: 11px;
      color: #4CBB17;
      cursor: pointer;
    ">📈 Game totals</button>

</div>

═══════════════════════════════════════
CHANGE 2 — app.py
ADD DATE AND DIVERSE PICKS INSTRUCTION
TO SYSTEM PROMPT
═══════════════════════════════════════

Find the /api/context-edge route.
Find where SYSTEM_PROMPT is built.
Find this line at the top of the
SYSTEM_PROMPT string:

"You are Mora Bets Context Edge Analyst."

Add today's date immediately after
the board data section.
Find the line that reads:

TODAY'S LIVE BOARD (no-vig calculated):
{format_board_for_claude(board)}

Change it to:

TODAY'S DATE: {__import__('datetime').datetime.now().strftime('%A %B %d %Y')}
TODAY'S LIVE BOARD (no-vig calculated):
{format_board_for_claude(board)}

This gives Claude the actual date so it
can reason about day of week, series
position, and schedule context.

Then find the OUTPUT FORMAT section
in the system prompt. Find the line
that says:

For each play use this format exactly:

Add this instruction BEFORE the format:

DIVERSITY RULE — REQUIRED:
When asked for multiple plays or
"best bets" or "top plays" you MUST
provide a mix across these categories.
Do not return only player props.

Required mix for any 5-play response:
- At least 1 moneyline or game spread
- At least 1 game total (over/under
  runs or goals)
- At least 1 player prop
- Remaining picks: best edge available
  regardless of category

For a 3-play response:
- At least 1 team bet (moneyline,
  spread, or total)
- At least 1 player prop
- 1 best available

For sport-specific requests:
If user asks for MLB value include MLB
moneylines, run lines, and totals
alongside player props.
If user asks for Soccer include match
result (moneyline 3-way: home/draw/away)
and total goals alongside player props.

MONEYLINE AND SPREAD ANALYSIS:
For team bets you must analyze:
- Starting pitcher ERA and recent form
- Home vs road splits for both teams
- Run differential last 10 games
- Bullpen availability and rest
- Park factor (run environment)
- The no-vig board's environment tag
  HIGH SCORING or LOW SCORING confirms
  the game total context
- Line value: if implied probability
  from the moneyline odds is below your
  context-adjusted probability, bet it

GAME TOTAL ANALYSIS:
For over/under game totals analyze:
- Both starters ERA and K rate
- Park factor from ENV tag on board
- Team run scored and allowed averages
- Weather if outdoor and relevant
- Recent scoring trends both teams

═══════════════════════════════════════
CONSTRAINTS
═══════════════════════════════════════

- Only touch dashboard.html quick
  prompt buttons in ce-messages
- Only touch the SYSTEM_PROMPT in
  app.py — add date line and diversity
  rule — nothing else in the route
- Do not touch format_board_for_claude
- Do not touch the fetch logic
- Do not touch the rate limit
- Do not touch any sport tab logic
- Do not touch mora_assists.py
- Do not touch scheduler jobs
- Do not touch Bovada footer
- Do not touch newsletter popup
- Do not touch line shop listener
- The datetime import uses inline
  __import__ to avoid adding a new
  import at module level — this is
  safe and requires no other changes