Read dashboard.html completely.
Find the sendContextMessage() function.
Do not change anything yet. Report:

1. Exact fetch call — what URL, what
   headers, what body is being sent

2. How is the response handled —
   find the res.json() call and what
   happens immediately after

3. Is there a try/catch wrapping the
   fetch — what does the catch block do

Then make these two surgical fixes only:

=== FIX 1: RESPONSE FORMAT MISMATCH ===

The backend returns:
{"response": "# 🔥 BEST BETS..."}

The frontend may be looking for
data.error or a different field name
and falling into the catch block.

Find this section inside
sendContextMessage() after:
const data = await res.json();

It should read the response as:
const text = data.response ||
  data.error ||
  'No response received.';

If it currently reads data.analysis
or data.result or data.message or
any other field name — that is the bug.
The field is data.response — fix it.

=== FIX 2: ADD CONSOLE LOGGING ===

Temporarily add console.log statements
so we can see exactly what is failing
in the browser console:

After const data = await res.json();
add:
console.log('CE status:', res.status);
console.log('CE data:', JSON.stringify(data));
console.log('CE response field:', data.response);

And in the catch block add:
console.log('CE catch error:', err.message, err.stack);

These let us see in browser devtools
exactly what the frontend receives.

=== FIX 3: VERIFY PASSCODE FLOW ===

Find the sessionStorage handling.
The frontend stores the passcode in
sessionStorage as 'ce_passcode'.
Every fetch to /api/context-edge must
include it in the request body.

Find the fetch body inside
sendContextMessage() and confirm it
includes:
passcode: sessionStorage.getItem('ce_passcode') || ''

If it reads from a variable that may
be stale or undefined — fix it to
read directly from sessionStorage
on every call.

Also find the _ceAuthed variable.
It is set to true after passcode
validation but it is a let variable
in JavaScript — it resets to false
on every page load.

The sessionStorage check at page load:
(function() {
  const stored = sessionStorage.getItem(
    'ce_passcode'
  );
  if (stored) _ceAuthed = true;
})();

Confirm this IIFE exists and runs
on page load. If it does not exist
add it — this is why auth may be
lost between sessions on the same
browser tab after a page refresh.

=== AFTER DEPLOY ===

Open morabets.com/dashboard in Chrome.
Open DevTools → Console tab.
Click the 🔍 bubble.
Enter passcode 4774.
Click "best bets today".

Report what appears in the console —
specifically the CE status, CE data,
and CE response field lines.
That output will show exactly where
the frontend breaks.

=== CONSTRAINTS ===
- Only touch dashboard.html
- Only the sendContextMessage function
  and the IIFE passcode restore
- Do not touch app.py
- Do not touch any sport tabs
- Do not touch mora_assists.py
- Do not touch scheduler jobs
- Do not touch Bovada footer
- Do not touch newsletter popup
- Do not touch line shop listener