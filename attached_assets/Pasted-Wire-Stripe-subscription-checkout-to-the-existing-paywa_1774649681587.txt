Wire Stripe subscription checkout to the existing paywall in this app.
The app already has a paywall page. I need:

Install stripe if not already: pip install stripe (add to requirements.txt)
Add a POST route /create-checkout-session to the backend (app.py or wherever routes live) that creates a Stripe Checkout session:

pythonimport stripe
import os

stripe.api_key = os.environ.get('STRIPE_SECRET_KEY')

@app.route('/create-checkout-session', methods=['POST'])
def create_checkout_session():
    try:
        session = stripe.checkout.Session.create(
            payment_method_types=['card'],
            mode='subscription',
            line_items=[{
                'price': os.environ.get('STRIPE_PRICE_ID'),
                'quantity': 1,
            }],
            success_url='https://morabets.com/success?session_id={CHECKOUT_SESSION_ID}',
            cancel_url='https://morabets.com/pricing',
        )
        return {'url': session.url}
    except Exception as e:
        return {'error': str(e)}, 500

Add a POST route /webhook to handle Stripe events and generate access keys:

python@app.route('/webhook', methods=['POST'])
def webhook():
    payload = request.get_data()
    sig_header = request.headers.get('Stripe-Signature')
    endpoint_secret = os.environ.get('STRIPE_WEBHOOK_SECRET')

    try:
        event = stripe.Webhook.construct_event(payload, sig_header, endpoint_secret)
    except Exception as e:
        return {'error': str(e)}, 400

    if event['type'] == 'checkout.session.completed':
        session = event['data']['object']
        email = session['customer_details']['email']
        access_key = generate_access_key()
        # Store the key however the app currently stores keys
        print(f'[ACCESS KEY] Generated {access_key} for {email}')

    return {'status': 'ok'}

def generate_access_key():
    import random, string
    chars = string.ascii_uppercase + string.digits
    key = 'MB-' + '-'.join(''.join(random.choices(chars, k=4)) for _ in range(4))
    return key

Find the Subscribe button on the paywall page. Wire it to call /create-checkout-session and redirect to the Stripe URL:

javascriptdocument.getElementById('subscribe-btn').addEventListener('click', async () => {
  const btn = document.getElementById('subscribe-btn')
  btn.textContent = 'Loading...'
  btn.disabled = true
  try {
    const res = await fetch('/create-checkout-session', { method: 'POST' })
    const data = await res.json()
    if (data.url) window.location.href = data.url
  } catch (e) {
    btn.textContent = 'Subscribe — $24.99/mo'
    btn.disabled = false
  }
})

Add a /success route that shows the access key after payment.
Add STRIPE_WEBHOOK_SECRET to the secrets — I will fill in the value after registering the webhook in Stripe dashboard.