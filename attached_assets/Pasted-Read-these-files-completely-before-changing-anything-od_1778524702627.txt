Read these files completely before
changing anything:
- odds_api.py (MLB fetch — mirror this)
- nhl_odds_api.py (NHL fetch — mirror this)
- app.py (scheduler — mirror this)
- dashboard.html (Soccer tab — add here)
- probability.py (no_vig — already works)

Report what you find in each file.
Specifically report:
- Exact function name that fetches MLB props
- Exact market keys used in MLB fetch
- Exact scheduler job registration pattern
- Exact cache file path pattern for MLB
- How no_vig_probability() is called
- How the dashboard tab switching works

Do NOT change anything yet.
Confirm your findings then proceed.

=== STEP 1: CREATE soccer_odds_api.py ===

Create a new file: soccer_odds_api.py
Mirror the exact structure of
nhl_odds_api.py replacing all NHL
references with soccer equivalents.

SOCCER LEAGUES TO FETCH:
Two sport keys in order of priority:

sport_key_1 = 'soccer_usa_mls'
sport_key_2 = 'soccer_fifa_world_cup'

For each league:
- Check if events exist before fetching
- If no events return empty list silently
- No error if league is out of season
- Log which league returned data

SOCCER PLAYER PROP MARKET KEYS:
These are the confirmed market keys
for The Odds API soccer player props.
Use exactly these strings:

'player_shots_on_target'
'player_shots'
'player_goals'
'player_assists'
'player_cards'

Fetch as comma-separated string:
markets = 'player_shots_on_target,player_shots,player_goals,player_assists,player_cards'

BOOKMAKERS — same as MLB/NHL:
bookmakers = 'draftkings,fanduel,betmgm,betrivers'

ODDS FILTER — lean calls only:
After fetching apply these filters
before caching. Do not store junk:

best_over_price >= -350
best_over_price <= 200
(American odds format)

This removes chalk props at -350+
and caps plus money at +200.
Matches PrizePicks board range exactly.

API CALL STRUCTURE:
Mirror the MLB event-by-event pattern
exactly as written in odds_api.py.

Step 1: Get events list
GET https://api.the-odds-api.com/v4/sports/{sport_key}/events
params: apiKey={ODDS_API_KEY}
        regions=us

Step 2: For each event get props
GET https://api.the-odds-api.com/v4/sports/{sport_key}/events/{event_id}/odds
params: apiKey={ODDS_API_KEY}
        regions=us
        markets={markets}
        oddsFormat=american
        bookmakers={bookmakers}

Step 3: Parse response
Mirror the MLB parsing exactly.
Extract for each player prop:
  player (description field)
  stat (market key)
  stat_label (human readable)
  line (point value)
  best_over_price
  best_book
  all_books list
  home_team
  away_team
  matchup
  sport = 'Soccer'
  league (MLS or FIFA World Cup)

Step 4: Calculate no_vig_probability
Call the same no_vig_probability()
function from probability.py
exactly as MLB does it.

Step 5: Cache to persistent disk
Cache file: /var/data/soccer_props_cache.json
Same JSON structure as mlb_props_cache.json

STAT LABEL MAPPING:
Convert market keys to human-readable:
'player_shots_on_target' → 'Shots on Target'
'player_shots'           → 'Total Shots'
'player_goals'           → 'Goals'
'player_assists'         → 'Assists'
'player_cards'           → 'Cards'

RATE LIMITING — critical:
Soccer has more events than NHL.
Add 0.5 second sleep between each
event API call to avoid 429 errors.

import time
time.sleep(0.5)

between each event fetch.
Mirror any existing retry logic
from odds_api.py exactly.

MAIN FUNCTION NAME:
def fetch_soccer_props():
    """
    Fetches MLS and FIFA World Cup
    player props. Returns combined list.
    Writes to soccer_props_cache.json.
    """

Returns: list of processed prop dicts
Logs: total events found per league,
      total props cached,
      API credits used


=== STEP 2: VERIFY BEFORE SCHEDULING ===

After creating soccer_odds_api.py
run this verification immediately:

python3 -c "
import json, os, sys
sys.path.insert(0, '.')
from soccer_odds_api import fetch_soccer_props

print('Fetching soccer props...')
props = fetch_soccer_props()
print(f'Total props returned: {len(props)}')

if props:
    print('Sample props:')
    for p in props[:5]:
        print(f'  {p.get(\"player\")} | {p.get(\"stat_label\")} | {p.get(\"line\")} | Over: {p.get(\"best_over_price\")} | No-vig: {p.get(\"no_vig_prob\")}%')
    
    # Verify no-vig calculated
    with_nv = [p for p in props if p.get('no_vig_prob', 0) > 0]
    print(f'Props with no-vig calculated: {len(with_nv)}')
    
    # Verify cache written
    cache = '/var/data/soccer_props_cache.json'
    if os.path.exists(cache):
        print(f'Cache written: {cache}')
        with open(cache) as f:
            cached = json.load(f)
        print(f'Cached props: {len(cached)}')
    else:
        print('WARNING: Cache not written')
else:
    print('No props returned — MLS may have no games today')
    print('FIFA World Cup may not be active')
    print('This is expected behavior off-season')
"

Report the output before proceeding.
If props returned with no_vig_prob
values above 0 — proceed to Step 3.
If empty — check if MLS has games
today. Empty is valid off-season.
Do not proceed to scheduling if
there are errors — only if empty
due to no games.


=== STEP 3: ADD TO SCHEDULER IN app.py ===

Only after Step 2 verification passes.

Find background_initializer() in app.py.
Find where MLB and NHL jobs are registered.

Add soccer job in same block:

scheduler.add_job(
    func=fetch_soccer_props_safe,
    trigger='cron',
    hour=7,
    minute=45,
    timezone='America/Phoenix',
    id='soccer_props_daily',
    name='Soccer Props 7:45AM PHX',
    replace_existing=True,
    misfire_grace_time=3600,
    coalesce=True
)
logger.info('Soccer props job registered')

7:45 AM PHX fires AFTER MLB (7:00 AM)
and NHL (7:15 AM) and board email (7:25 AM).
Staggers API calls. Avoids rate limiting.

Add the safe wrapper function to app.py:

def fetch_soccer_props_safe():
    try:
        from soccer_odds_api import (
            fetch_soccer_props
        )
        props = fetch_soccer_props()
        logger.info(
            f'Soccer props fetched: '
            f'{len(props)} props cached'
        )
    except Exception as e:
        logger.error(
            f'Soccer props fetch failed: {e}'
        )

Also add soccer cache to the board
email job that runs at 7:25 AM PHX.
The board email currently loads:
  mlb_props_cache.json
  nhl_props_cache.json

Add:
  soccer_props_cache.json

So the board email includes
soccer props alongside MLB and NHL.


=== STEP 4: ADD SOCCER TAB TO DASHBOARD ===

Find the sport tab row in dashboard.html.
Currently shows: MLB | NFL | NHL

Add Soccer tab after NHL:

<button class="sport-tab"
        data-sport="Soccer"
        onclick="filterBySport('Soccer')"
        style="[mirror exact style of
               existing sport tab buttons]">
  ⚽ Soccer
</button>

Find the JavaScript function that
filters props by sport.
It currently filters on sport field
matching 'MLB' 'NFL' 'NHL'.

Add 'Soccer' to the same filter logic.
The soccer props cache uses
sport = 'Soccer' in the prop dict
so it will filter correctly.

Also add Soccer to any sport
dropdown or selector if one exists
in dashboard.html.


=== STEP 5: ADD SOCCER CACHE TO
/api/props ENDPOINT IN app.py ===

Find the route that serves props
to the dashboard frontend.
It likely loads mlb and nhl caches
and combines them.

Add soccer cache to that same load:

soccer_cache = '/var/data/soccer_props_cache.json'
soccer_props = []
if os.path.exists(soccer_cache):
    with open(soccer_cache) as f:
        soccer_props = json.load(f)

Then include soccer_props in the
combined response alongside mlb and nhl.


=== CONSTRAINTS ===
- Create soccer_odds_api.py as new file
- Only add to app.py — do not touch
  any existing MLB or NHL functions
- Only add Soccer tab to dashboard.html
  do not touch other tabs or UI
- Do not touch mora_assists.py
- Do not touch email gate
- Do not touch Meta pixel
- Do not touch color scheme
- 0.5 second sleep between event calls
- Filter: -350 to +200 American odds
- Cache path: /var/data/soccer_props_cache.json
- sport field value: 'Soccer'
- Verify step runs before scheduling
- Report verification output before
  proceeding to Steps 3-5
- If MLS has no games today empty
  cache is valid — do not treat as error
- FIFA World Cup sport key included
  but handled gracefully if not active